# Общие зависимости (обе платформы)
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
numpy>=1.26
sounddevice>=0.4.7
soxr>=0.4
# faster-whisper нужен на обеих платформах: Silero VAD (onnx) + CPU-фолбэк ASR.
# На Windows/NVIDIA он же — основной ASR-бэкенд.
faster-whisper>=1.0.3,<2
pywebview>=5.1
