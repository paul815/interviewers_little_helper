# The minimum for the unit and API tests (no ASR, no audio hardware, no Ollama).
# The versions are pinned for the same reason as in requirements-common.txt.
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
pydantic==2.13.4
numpy==2.5.1
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.3
